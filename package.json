{
  "name": "fantasy-map-generator",
  "version": "1.0.0",
  "description": "A React App that generates random fantasy maps with addition to receiving minimal user input",
  "engines": {
    "node": "6.11.1"
  },
  "scripts": {
    "start": "node server",
    "heroku-postbuild": "cd react-ui/ && npm install --only=dev && npm install && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "react-ui/node_modules"
  ],
  "dependencies": {
    "axios": "^0.16.1",
    "express": "^4.14.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/kubarium/fantasy-map-generator.git"
  },
  "keywords": [
    "node",
    "heroku",
    "create-react-app",
    "react"
  ],
  "license": "MIT",
  "devDependencies": {
    "gulp-watch": "^4.3.11"
  }
}
